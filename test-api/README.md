# Dummy API for Edbank

This project serves as testing API for Edbank frontend. It is written as simple Node.js server application.

## Installing

For install run `npm install`

## Running

To start server run `npm start`.
